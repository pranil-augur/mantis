# kind-config.yaml
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker
containerdConfigPatches:
  - |-
    [plugins."io.containerd.grpc.v1.cri".registry.configs."registry.gitlab.com".auth]
      username = "gitlab+deploy-token-5242993"
      password = "gldt-wxu8w4uDucBry_EsUM6a"

